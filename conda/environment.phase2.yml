name: justnews-py312-phase2
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12.11
  - pip
  - gcc_linux-64
  - gxx_linux-64
  
  # Scientific Stack (CPU-only)
  - numpy>=1.26
  - scipy>=1.11
  - pandas>=2.0
  - scikit-learn>=1.3
  - networkx>=3.0
  - numba

  # Clustering & Dimensionality Reduction
  - hdbscan>=0.8.33
  - umap-learn>=0.5.5

  # Database
  - mysql-connector-python>=9.0
  - sqlalchemy>=2.0
  - aiosqlite>=0.19

  # Web Frameworks
  - fastapi>=0.120
  - uvicorn
  - starlette
  - django>=4.2

  # Networking
  - requests>=2.31
  - httpx>=0.28
  - aiohttp>=3.9
  - aiofiles
  - uvloop
  - httptools
  - watchfiles

  # Metrics & Monitoring (installed via pip)
  # - prometheus-client>=0.19

  # Authentication & Security (installed via pip)
  # - pyjwt>=2.8

  # Utils
  - python-dotenv>=1.0
  - orjson
  - ujson
  - protobuf
  - thrift

  # Parsing
  - trafilatura
  - readability-lxml
  - justext
  - extruct
  - w3lib
  - langdetect
  - lxml
  - dateparser
  - beautifulsoup4
  - html5lib
  - cssselect
  - tld
  - lxml-html-clean

  # Opentelemetry
  - opentelemetry-api
  - opentelemetry-sdk
  - opentelemetry-instrumentation
  - opentelemetry-instrumentation-fastapi
  - opentelemetry-instrumentation-requests

  # Testing
  - pytest
  - pytest-asyncio
  - pytest-cov
  - coverage
  - mypy
  - ruff

  - pip:
    # Metrics & Authentication
    - prometheus-client>=0.19
    - pyjwt>=2.8
    
    # Vector Store (query-only)
    - chromadb>=0.4
    
    # Utilities
    - datasets
    - litellm
    - tiktoken>=0.12
