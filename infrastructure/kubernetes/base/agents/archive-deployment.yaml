apiVersion: apps/v1
kind: Deployment
metadata:
  name: archive
  namespace: justnews
  labels:
    app: archive
    component: agent
    gpu-required: false
spec:
  replicas: 1
  selector:
    matchLabels:
      app: archive
  template:
    metadata:
      labels:
        app: archive
        component: agent
        gpu-required: false
    spec:
      containers:
      - name: archive
        image: justnews/archive:latest
        ports:
        - containerPort: 8012
          name: http
        envFrom:
        - configMapRef:
            name: justnews-config
        - secretRef:
            name: justnews-secrets
        env:
        - name: AGENT_NAME
          value: "archive"
        - name: AGENT_PORT
          value: "8012"
        resources:
          limits:
            memory: 2Gi
            cpu: 1000m
          requests:
            memory: 1Gi
            cpu: 500m
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 6
        readinessProbe:
          httpGet:
            path: /ready
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3

---
apiVersion: v1
kind: Service
metadata:
  name: archive-service
  namespace: justnews
  labels:
    app: archive
    component: agent
spec:
  selector:
    app: archive
  ports:
  - port: 8012
    targetPort: http
    name: http
  type: ClusterIP

---
