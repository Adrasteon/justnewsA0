apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics
  namespace: justnews
  labels:
    app: analytics
    component: agent
    gpu-required: false
spec:
  replicas: 1
  selector:
    matchLabels:
      app: analytics
  template:
    metadata:
      labels:
        app: analytics
        component: agent
        gpu-required: false
    spec:
      containers:
      - name: analytics
        image: justnews/analytics:latest
        ports:
        - containerPort: 8012
          name: http
        envFrom:
        - configMapRef:
            name: justnews-config
        - secretRef:
            name: justnews-secrets
        env:
        - name: AGENT_NAME
          value: "analytics"
        - name: AGENT_PORT
          value: "8012"
        resources:
          limits:
            memory: 2Gi
            cpu: 1000m
          requests:
            memory: 1Gi
            cpu: 500m
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 6
        readinessProbe:
          httpGet:
            path: /ready
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3

---
apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: justnews
  labels:
    app: analytics
    component: agent
spec:
  selector:
    app: analytics
  ports:
  - port: 8012
    targetPort: http
    name: http
  type: ClusterIP

---
