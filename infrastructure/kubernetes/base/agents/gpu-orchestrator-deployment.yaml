apiVersion: apps/v1
kind: Deployment
metadata:
  name: gpu-orchestrator
  namespace: justnews
  labels:
    app: gpu-orchestrator
    component: agent
    gpu-required: false
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gpu-orchestrator
  template:
    metadata:
      labels:
        app: gpu-orchestrator
        component: agent
        gpu-required: false
    spec:
      containers:
      - name: gpu-orchestrator
        image: justnews/gpu-orchestrator:latest
        ports:
        - containerPort: 8015
          name: http
        envFrom:
        - configMapRef:
            name: justnews-config
        - secretRef:
            name: justnews-secrets
        env:
        - name: AGENT_NAME
          value: "gpu-orchestrator"
        - name: AGENT_PORT
          value: "8015"
        resources:
          limits:
            memory: 2Gi
            cpu: 1000m
          requests:
            memory: 1Gi
            cpu: 500m
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 6
        readinessProbe:
          httpGet:
            path: /ready
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3

---
apiVersion: v1
kind: Service
metadata:
  name: gpu-orchestrator-service
  namespace: justnews
  labels:
    app: gpu-orchestrator
    component: agent
spec:
  selector:
    app: gpu-orchestrator
  ports:
  - port: 8015
    targetPort: http
    name: http
  type: ClusterIP

---
