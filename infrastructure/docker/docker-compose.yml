# Compatibility pointer for historical path.
# Canonical Docker runtime file:
#   infrastructure/docker/docker-compose.canonical.yml
#
# Use:
#   JUSTNEWS_COMPOSE_FILE=infrastructure/docker/docker-compose.canonical.yml \
#   bash scripts/ops/docker_compose.sh up

include:
  - ./docker-compose.canonical.yml
