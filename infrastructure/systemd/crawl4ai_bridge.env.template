
## Environment template for crawl4ai-bridge.service
## The project also includes a `global.env` at the repository root. You can either
## copy values from this template into `/home/adra/JustNews/global.env` or
## maintain a separate env file and point the systemd unit to it.

## Crawl4AI host/port used by agents and the bridge (defaults used if not set)
CRAWL4AI_HOST=127.0.0.1
CRAWL4AI_PORT=3308

## Optional: full base URL (overrides host/port)
CRAWL4AI_BASE_URL=

## Whether to enable LLM extraction by default
CRAWL4AI_USE_LLM=true

## Model cache directory used by Crawl4AI (adjust to your storage)
CRAWL4AI_MODEL_CACHE_DIR=/var/lib/justnews/crawl4ai_model_cache

## Python runtime (optional)
PYTHON_BIN=/home/adra/JustNews/.venv/bin/python

## Logging level for uvicorn
LOG_LEVEL=info
