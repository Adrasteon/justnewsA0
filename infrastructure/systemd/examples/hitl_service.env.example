# Environment overrides for justnews@hitl_service
# Copy to /etc/justnews/hitl_service.env and adjust values as needed.

# Bind address/port for the FastAPI service
HITL_SERVICE_HOST=0.0.0.0
HITL_SERVICE_PORT=8040
HITL_SERVICE_LOG_LEVEL=info

# External address announced to the MCP Bus (must be reachable by the bus)
HITL_SERVICE_ADDRESS=http://127.0.0.1:8040

# Local SQLite staging DB path used by the HITL service. Override to a different
# absolute path for production or shared DBs.
HITL_DB_PATH=agents/hitl_service/hitl_staging.db

# Enable/disable pipeline: if false the crawler will not POST candidates.
# Default: true
ENABLE_HITL_PIPELINE=true

# How often (seconds) the HITL service will emit stats to logs (and for the
# crawler to optionally poll). Set to 0 to disable periodic stats.
HITL_STATS_INTERVAL_SECONDS=30

# Backoff for failed HITL submissions from the crawler (seconds).
HITL_FAILURE_BACKOFF_SECONDS=60

# MCP Bus configuration (inherits defaults from global.env when not set)
# MCP_BUS_URL=http://127.0.0.1:8000
# HITL_AGENT_NAME=hitl_service

# Optional forwarding targets; uncomment to enable automatic dispatch
# HITL_FORWARD_AGENT=ingest_service
# HITL_FORWARD_TOOL=queue_article
# HITL_TRAINING_FORWARD_AGENT=training_system
# HITL_TRAINING_FORWARD_TOOL=receive_hitl_label
