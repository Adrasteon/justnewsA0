### Example global.env for JustNews native deployment
# Copy this to /etc/justnews/global.env and edit values for your host

# Service user and group
SERVICE_USER=adra
SERVICE_GROUP=adra

# Path to the repository (update to your checkout root)
SERVICE_DIR=/home/adra/JustNews

# Python environment (activate or full path)
PYTHON_BIN=/home/adra/miniconda3/envs/${CANONICAL_ENV:-justnews-py312}/bin/python

# Optional: explicitly prefer a canonical interpreter path (useful when conda binaries are broken)
# CANONICAL_PYTHON_PATH=/home/adra/miniconda3/envs/${CANONICAL_ENV:-justnews-py312}/bin/python

# Optional strict enforcement - if set to 1, service startup will fail unless the resolved
# interpreter matches the canonical conda env. Useful for production hosts where drift is not allowed.
# ENFORCE_CANONICAL_PYTHON=0

# Optional ModelStore root: when set, agents will prefer canonical per-agent
# model directories. Use an absolute path on a shared filesystem.
# Example: /media/adra/Data/justnews/model_store
MODEL_STORE_ROOT=/media/adra/Data/justnews/model_store

# Recommendation: create the model store and set group perms so trainers can
# write and agents can read. Example commands run as root:
#   mkdir -p /opt/justnews/models
#   chgrp -R justnews /opt/justnews/models
#   chmod -R g+rwX /opt/justnews/models

# Optional: where to write logs
LOG_DIR=/var/log/justnews

# Enable HTTP fetching for the crawler by default
UNIFIED_CRAWLER_ENABLE_HTTP_FETCH=true

# MCP bus wait / startup helper paths
WAIT_FOR_MCP=/usr/local/bin/wait_for_mcp.sh
START_AGENT=/usr/local/bin/justnews-start-agent.sh

# NVIDIA MPS Configuration (Enterprise GPU Isolation)
# Enable MPS for GPU resource isolation across agents
# Requires: nvidia-cuda-mps-control daemon running
ENABLE_MPS=true

# Optional, secure API token for the crawler and Crawl4AI bridge
# If set, internal endpoints will require the token to be provided via
# Authorization: Bearer <token> or X-Api-Token header.
CRAWLER_API_TOKEN=
